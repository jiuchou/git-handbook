# 1.2.常用命令

```bash
# 删除远程分支
git push origin :delete_branch_name
# 添加仓库
git remote add origin http://github.com/xxx/xxx.git

# 查看
git status

# 添加
git add --all
git add .
git add file

# 撤销add操作
git reset HEAD
git reset HEAD file
```

